"""Promotion loops — Kora's self-improving subsystems.

Per ``feedback-promotion-loops-self-improving-subsystems``: Kora
observes recurring patterns, drafts proposals, operator approves
via cockpit, audit captures the loop end-to-end. First concrete
implementation is :mod:`.phrasebook`; future loops (snapshot-expand,
router-trigger, tool-trimming, probe-fix-envelope) follow the same
pattern.
"""
